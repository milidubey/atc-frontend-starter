import { Button } from 'antd';

const BtnComponent = () => {
    return (
        <Button type="primary" htmlType="submit">
            {'Submit'}
        </Button>
    );
};

export default BtnComponent;